# BG-cleaner configuration
# All variables are prefixed with BGC_ and are optional (sensible defaults exist).

# Directory containing ONNX model files
# BGC_MODELS_DIR=assets/models

# ONNX Runtime execution providers (comma-separated JSON list)
# CPU only (default):
# BGC_ONNX_PROVIDERS=["CPUExecutionProvider"]
# GPU with CPU fallback (requires onnxruntime-gpu + nvidia-cudnn-cu12):
# BGC_ONNX_PROVIDERS=["CUDAExecutionProvider","CPUExecutionProvider"]

# Maximum upload size in MB
# BGC_MAX_UPLOAD_MB=10.0